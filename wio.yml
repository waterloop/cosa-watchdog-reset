type: pkg

project:
  name: cosa-watchdog-reset
  version: 1.0.0
  keywords:
  - wio
  - pkg
  compile_options:
    wio_version: 0.10.0
    default_target: tests

targets:
  tests:
    src: tests
    platform: avr
    framework: cosa
    board: mega2560
